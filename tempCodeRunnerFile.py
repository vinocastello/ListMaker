            # elif command == "weather":
            #     while True:
            #         loc = await client.wait_for("message",check=lambda msg: msg.author == client.user)
            #         if len(loc) > 0:
            #             break
            #     message.channel.send(f"loc = {loc}")